# Copy to .env and fill in your keys.
# .env is never committed — secrets stay out of version control.
# providers.json uses ${VAR} placeholders that are resolved from these values at startup.

ANTHROPIC_API_KEY=sk-ant-...
GROQ_API_KEY=gsk_...
